<%= render 'shared/title', title: '新增客戶' %>

<div>
  <%= form_for @customer do |f| %>
    
    <p>客戶名稱</p>
    <p><%= f.text_field :name %></p>

    <p>承辦人</p>
    <p><%= f.select :officer_id, options_for_select(@users) %></p>

    <p>領組</p>
    <p><%= f.select :leader_id, options_for_select(@users) %></p>

    <p>經理</p>
    <p><%= f.select :manager_id, options_for_select(@users) %></p>
    
    <p><%= f.submit '新增' %></p>
  <% end %>
</div>